---
title: Exercise 32.4
tags:
  - problem
  - induction
---
Previous: [[ross analysis/32/3|Exercise 32.3]]
## Supply the induction argument needed in the proof of Lemma 32.2.

Lemma 32.2 says:

*Let $f$ be a bounded function on $[a,b]$. If $P$ and $Q$ are partitions of $[a,b]$ and $P \subseteq Q$, then*

$$
L(f,P) \leq L(f,Q) \leq U(f,Q) \leq U(f,P) \tag{1}
$$

The proof is as follows:

The middle inequality is obvious. The proofs of the first and third inequalities are similar, so we prove

$$
L(f,P)\leq L(f,Q). \tag{2}
$$

We shall prove this by an induction argument on the number of points $Q$ has more than $P$.

First we consider the case when $Q$ has only one more point, say $u$, than $P$. If

$$
P=\{a=t_0<t_1<\cdots<t_n=b \}
$$

then

$$
Q = \{a=t_0<t_1<\cdots<t_{k-1}<u<t_k<\cdots<t_n=b\}
$$

for some $k\in\{1,2,\ldots,n\}$. The lower Darboux sums for $P$ and $Q$ are the same except for the terms involving $t_{k-1}$ or $t_k$. In fact, their difference is

$$
\begin{align*}
L(f,Q)-L(f,P)&=m(f,[t_{k-1},u])\cdot(u-t_{k-1})+m(f,u,t_k])\cdot(t_k-u)-m(f,t_{k-1},t_k])\cdot(t_k-t_{k-1}) \\
&\geq m(f,[t_{k-1},t_k])\cdot(u-t_{k-1})+m(f,[t_{k-1},t_k])\cdot(t_k-u)-m(f,t_{k-1},t_k])\cdot(t_k-t_{k-1}) \\
&= m(f,[t_{k-1},t_k])\cdot(u-t_{k-1}+t_k-u-t_k+t_{k-1}) \\
&=0
\end{align*}
$$

Assume this result is true when $Q$ has $k$ points more than $P$. We shall denote this $Q$ by $Q_k$ to emphasize the fact that it has $k$ more points and write

$$
L(f,P) \leq L(f,Q_k) \tag{1}
$$

Now let $Q_{k+1}$ be a partition with 1 point more than $Q$ (and $k+1$ point more than $P$). Using the result for 1 more point

$$
L(f,Q_k) \leq L(f,Q_{k+1}) \tag{2}
$$

From $(1)$ and $(2)$ we get the result for $k+1$ points

$$
L(f,P) \leq L(f,Q_{k+1})
$$

And thus induction is complete.

$$
\ \tag*{$\blacksquare$}
$$

Next: [[ross analysis/32/5|Exercise 32.5]]